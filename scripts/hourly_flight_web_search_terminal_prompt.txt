Use the installed skill `flight-hourly-web-search`.

Current run inputs:
- `origin_airports`: `{origin_airports}`
- `destination_scope`: `{destination_scope}`
- `top_n`: `{top_n}`
- **Travel "today" (cutoff)**: `{today_iso}` — `{today_long}` (all travel dates must be on or after this calendar day in the search timezone).

Extra notes:
{notes}

This run is for a local terminal/dashboard workflow.

Hard requirements:
1. Use web search only.
2. Do not call any paid API.
3. Do not use browser automation or Browser Use.
4. Output the required fenced JSON block first.
5. After the JSON block, add a short Markdown summary for terminal display.
6. Do not send anything to chat. Print the result only to stdout.
7. You must actively search every airport listed in `origin_airports`; do not collapse the run to only the first origin.
8. When results exist, try to include at least one credible fare from each origin before filling the rest with the overall cheapest routes.
9. **Dates**: Only surface round-trip fares that can still be booked for outbound travel **on or after `{today_iso}`**. Ignore expired sales, archive pages, "last month" blog posts, or itineraries whose trip dates are entirely before `{today_iso}`. Prefer stating `date_range` with explicit **YYYY-MM-DD** when the source gives it.
